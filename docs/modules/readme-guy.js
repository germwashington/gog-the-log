// Readme Guy - UI & Initialization
// Module 3: Manages startup screen, tutorial, game initialization

class ReadmeGuy {
    constructor(config = {}, eventBus = null, dataStore = null) {
        this.config = config;
        this.eventBus = eventBus;
        this.dataStore = dataStore;
    }
}

export { ReadmeGuy };

